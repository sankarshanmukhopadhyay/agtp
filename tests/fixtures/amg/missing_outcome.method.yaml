# Invalid: the AGIS semantic block omits the required ``outcome`` field.
# The composer rejects this at its coherence layer (before validation).

name: ASSESS
semantic:
  intent: Assesses input against a ruleset
  actor: agent
  # outcome deliberately absent
  capability: analysis
  is_idempotent: true

category: transact
required_params:
  - name: input
    type: object
    description: data to assess
    schema:
      type: object

error_codes: [400, 422]

source: amg/1.0
namespace: acme-quality
