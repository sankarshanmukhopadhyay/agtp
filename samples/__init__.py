"""
Sample handler implementations referenced by the TOML files in
``endpoints/``. These are documentation-grade — copy them when
authoring a real server. The directory ships with the
package source so the smoke-test loader can resolve the references.
"""

from __future__ import annotations
